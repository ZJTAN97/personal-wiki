---
title: 643. Maximum Average Subarray 1
---

import { Badge } from "@astrojs/starlight/components";

Difficulty: <Badge text="EASY" variant="success" />

Problem description can be found [here](https://leetcode.com/problems/maximum-average-subarray-i).

## Approach

Fixed Sliding Window

## Solution

```ts
function findMaxAverage(nums: number[], k: number): number {
  if (nums.length === 1) return nums[0];

  let window = nums.slice(0, k).reduce((a, b) => a + b, 0);
  let maxAverage = window / k;

  for (let i = k; i < nums.length; i++) {
    window = window + nums[i] - nums[i - k];
    maxAverage = Math.max(maxAverage, window / k);
  }

  return maxAverage;
}

findMaxAverage([1, 12, -5, -6, 50, 3], 4); // 12.7500
findMaxAverage([0, 1, 1, 3, 3], 4); // 2.0000
```
